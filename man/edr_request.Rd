% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/request.R
\name{edr_request}
\alias{edr_request}
\title{Perform a low-level EDR request}
\usage{
edr_request(
  client,
  path,
  query = list(),
  format = c("json", "geojson", "covjson", "csv", "html", "raw"),
  parse = TRUE
)
}
\arguments{
\item{client}{An \code{edr_client} from \code{\link[=edr_client]{edr_client()}}.}

\item{path}{Path under the base URL (with or without leading slash),
e.g. \code{"collections/monitoring-locations/locations"}.}

\item{query}{Named list of query parameters. Values may be scalars
or vectors; vectors are joined with \code{","}. \code{NULL} entries are
dropped.}

\item{format}{Response format: one of \code{"json"} (default), \code{"geojson"},
\code{"covjson"}, \code{"csv"}, \code{"html"}, or \code{"raw"}. For compatibility with
current pygeoapi deployments, JSON, GeoJSON, and CoverageJSON default to
\code{?f=json} and use the \code{Accept} header plus response body to distinguish
encodings. An explicit \code{query$f} is always honored, so a strict endpoint's
advertised token can be requested with, for example,
\code{query = list(f = "CoverageJSON")} while retaining CoverageJSON parsing.}

\item{parse}{If \code{TRUE} (default), parses JSON / GeoJSON / CovJSON
bodies into R structures. If \code{FALSE}, returns the raw \code{httr2}
response.}
}
\value{
A parsed list, tibble, or \code{edr_response} wrapper; an
\code{httr2_response} when \code{parse = FALSE}; or a typed empty result for HTTP
204 responses.
}
\description{
Generally you should not need to call this directly: the high-level
verbs (\code{\link[=edr_locations]{edr_locations()}}, \code{\link[=edr_area]{edr_area()}}, etc.) build the path and
query string for you. Use \code{\link[=edr_request]{edr_request()}} when you need to hit a
bespoke path or a non-standard parameter.
}
