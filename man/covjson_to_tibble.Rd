% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parsers.R
\name{covjson_to_tibble}
\alias{covjson_to_tibble}
\title{Convert a CoverageJSON response to a tidy tibble}
\usage{
covjson_to_tibble(x, datetime_as_posix = TRUE)
}
\arguments{
\item{x}{A CoverageJSON object: either an \code{edr_response} returned by
\code{\link[=edr_location]{edr_location()}} / \code{\link[=edr_area]{edr_area()}} / \code{\link[=edr_cube]{edr_cube()}} (etc.) with
\code{format = "covjson"}, or the raw parsed list.}

\item{datetime_as_posix}{If \code{TRUE} (default), attempts to parse the
time axis to \code{POSIXct} (UTC). Falls back to character on failure.}
}
\value{
A tibble with columns \code{coverage_id}, \code{parameter},
\code{parameter_label}, \code{unit}, \code{datetime}, \code{x}, \code{y}, \code{z}, and \code{value}.
Columns that are absent from the source are filled with \code{NA}.
}
\description{
Flattens a CoverageJSON \code{Coverage} or \code{CoverageCollection} into a
long tibble with one row per (coverage, parameter, time-step). Handles
the \code{Point} and \code{PointSeries} domain types used by station-based EDR
providers, and falls back to a general N-dimensional unrolling for
\code{Grid}-like domains.
}
