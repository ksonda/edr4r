% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capabilities.R
\name{edr_capabilities}
\alias{edr_capabilities}
\title{Inspect advertised EDR capabilities}
\usage{
edr_capabilities(
  client,
  collection_id = NULL,
  instance_id = NULL,
  refresh = FALSE
)
}
\arguments{
\item{client}{An \code{\link[=edr_client]{edr_client()}}.}

\item{collection_id}{Optional collection identifier.}

\item{instance_id}{Optional instance identifier. Requires \code{collection_id}.}

\item{refresh}{If \code{TRUE}, bypass and replace cached discovery metadata.}
}
\value{
An \code{edr_capabilities} object with a scope-specific subclass:
\code{edr_service_capabilities}, \code{edr_collection_capabilities}, or
\code{edr_instance_capabilities}. Service snapshots contain \code{landing},
\code{conformance}, and \code{collections}. Collection and instance snapshots
contain raw metadata, a normalized one-row \code{summary}, \code{queries},
\code{parameters}, \code{output_formats}, and \code{output_crs}.
}
\description{
\code{edr_capabilities()} gathers discovery metadata without probing any data
query endpoint. With neither id it returns a service snapshot. With a
\code{collection_id} it returns a collection snapshot, and with both a
\code{collection_id} and \code{instance_id} it returns an instance snapshot.
}
\details{
Discovery responses use the client's process-local in-memory cache. Set
\code{refresh = TRUE} to bypass matching entries, or call \code{\link[=edr_cache_clear]{edr_cache_clear()}}.
Capability values report what the server advertises; they do not prove that
the corresponding query succeeds.
}
