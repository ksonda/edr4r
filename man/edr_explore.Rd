% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{edr_explore}
\alias{edr_explore}
\title{One-shot fetch + plot + map for a collection}
\usage{
edr_explore(
  client,
  collection_id,
  bbox = NULL,
  coords = NULL,
  datetime = NULL,
  parameter_name = NULL,
  limit = NULL,
  record_limit = NULL,
  max_requests = 100L,
  file = NULL,
  popup = "plot+csv",
  method = c("auto", "cube", "area", "position", "per-location"),
  output = c("auto", "map", "plot", "data"),
  plot_view = c("auto", "time", "profile", "grid"),
  quiet = FALSE,
  ...,
  instance_id = NULL
)
}
\arguments{
\item{client}{An \code{edr_client}.}

\item{collection_id}{Collection identifier.}

\item{bbox}{Optional numeric length-4 bbox. Used both to filter
the locations index (if the server honours it) and as the bbox
for the cube fetch in \code{method = "auto"}. If omitted with
\code{method = "cube"}, derived from the bounding box of the returned
locations sf.}

\item{coords}{Point coords for \code{position}, or polygon coords for
\code{area}. Forwarded to \code{\link[=edr_position]{edr_position()}} / \code{\link[=edr_area]{edr_area()}}.}

\item{datetime}{ISO-8601 interval forwarded to the data fetch.}

\item{parameter_name}{Character vector of parameter ids; forwarded
to the data fetch. Use \code{\link[=edr_parameters]{edr_parameters()}} to discover valid ids.}

\item{limit}{Optional cap on the number of stations to map.}

\item{record_limit}{Optional per-station record cap, passed through
to \code{\link[=edr_location]{edr_location()}} in the per-location path. Useful for servers
(e.g. USGS waterdata) that cap responses at ~10 records by
default. Ignored on the cube and area paths.}

\item{max_requests}{Maximum number of per-location data requests permitted
in one call. Defaults to 100. Set to \code{Inf} only when an intentionally
unbounded batch is acceptable. Ignored by bulk methods.}

\item{file}{If non-\code{NULL}, write the map to this HTML path via
\code{\link[=edr_save_html]{edr_save_html()}} and return \code{file} invisibly. Otherwise return
the \code{leaflet} map.}

\item{popup}{Popup mode (forwarded to \code{\link[=edr_map]{edr_map()}}).}

\item{method}{One of \code{"auto"} (default), \code{"cube"}, \code{"area"},
\code{"position"}, or \code{"per-location"}. See above.}

\item{output}{One of \code{"auto"} (default), \code{"map"}, \code{"plot"}, or
\code{"data"}. \code{"auto"} returns a station map for station time-series
results and an interactive coverage map for gridded/profile
results.}

\item{plot_view}{Plot view passed to \code{\link[=edr_plot]{edr_plot()}} when returning a
plot. Defaults to \code{"auto"}.}

\item{quiet}{If \code{FALSE} (default), print a cli progress bar when
falling back to per-location fetches.}

\item{...}{Forwarded to \code{\link[=edr_map]{edr_map()}} when returning a map.}

\item{instance_id}{Optional collection instance identifier. When supplied,
capability planning and every locations/data request use that instance.
This keyword-only argument leaves existing positional calls unchanged.}
}
\value{
A \code{leaflet} htmlwidget, a \code{ggplot}, a tidy tibble/list when
\code{output = "data"}, or \code{invisible(file)} when a map is saved.
}
\description{
Convenience wrapper that plans a supported query, fetches data with
\strong{one} bulk request via \code{\link[=edr_cube]{edr_cube()}} or \code{\link[=edr_area]{edr_area()}} when possible,
and hands the result to \code{\link[=edr_map]{edr_map()}} or \code{\link[=edr_plot]{edr_plot()}}. Station locations
are requested only when the result needs a station map or a per-location
fallback. Optionally writes a map to a self-contained HTML file.
}
\details{
The default \code{method = "auto"} picks the cheapest route the
collection advertises in its \code{data_queries}:
\itemize{
\item \strong{cube} -- one HTTP call returning a CoverageCollection across
the whole bbox. Fast. Used when the collection supports \code{cube}
\emph{and} a \code{bbox} is supplied.
\item \strong{area} -- like cube but uses a polygon. Used when \code{coords} is
supplied and the collection supports \code{area}.
\item \strong{position} -- one HTTP call at a point. Useful for vertical
profiles returned by position queries.
\item \strong{per-location} -- the fallback: one HTTP call per station via
\code{\link[=edr_location]{edr_location()}}. Slower (N+1), used when neither spatial bulk
query is supported or the matching spatial input was not supplied.
}

Force a specific path by setting \code{method}. \code{coords} is required for
\code{area} and \code{position}; if \code{method = "cube"} and \code{bbox} is omitted,
the bbox is derived from the returned locations.
}
\examples{
\dontrun{
cl <- edr_client("https://api.wwdh.internetofwater.app")

# One /cube call across a bbox -- fast.
edr_explore(
  cl, "rise-edr",
  bbox           = c(-116, 35.5, -114, 36.5),
  datetime       = "2023-01-01/2023-03-31",
  parameter_name = "3",
  file           = tempfile(fileext = ".html")
)
}
}
