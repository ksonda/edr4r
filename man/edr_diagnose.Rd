% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capabilities.R
\name{edr_diagnose}
\alias{edr_diagnose}
\title{Diagnose an EDR endpoint's discovery surface}
\usage{
edr_diagnose(client, collection_id = NULL, instance_id = NULL, refresh = TRUE)
}
\arguments{
\item{client}{An \code{\link[=edr_client]{edr_client()}}.}

\item{collection_id}{Optional collection identifier.}

\item{instance_id}{Optional instance identifier. Requires \code{collection_id}.}

\item{refresh}{If \code{TRUE} (default), perform fresh probes and replace
successful cache entries. Use \code{FALSE} to permit cached metadata.}
}
\value{
A tibble with stable \code{check}, \code{status} (\code{"pass"}, \code{"warn"},
\code{"fail"}, or \code{"skip"}), and \code{detail} columns.
}
\description{
Performs small, read-only metadata requests and reports each check rather
than stopping at the first network or schema failure. No data query is
issued. Argument errors still stop immediately.
}
