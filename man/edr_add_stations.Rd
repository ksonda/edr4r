% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map-stations.R
\name{edr_add_stations}
\alias{edr_add_stations}
\title{Add station layers to an EDR map}
\usage{
edr_add_stations(
  map,
  locations,
  data = NULL,
  popup = c("plot+csv", "plot", "csv", "table", "all"),
  location_col = "coverage_id",
  id_col = NULL,
  label_col = NULL,
  parameter = NULL,
  plot_width = 7,
  plot_height = 3.5,
  plot_dpi = 72,
  marker_radius = 6,
  matched_color = "#2C7FB8",
  unmatched_color = "#BBBBBB",
  show_unmatched = TRUE,
  legend = TRUE,
  max_match_distance = NULL,
  group = "Stations",
  fit = FALSE
)
}
\arguments{
\item{map}{A \code{leaflet} htmlwidget, typically returned by \code{\link[=edr_map]{edr_map()}}.}

\item{locations}{An \code{sf} object from \code{\link[=edr_locations]{edr_locations()}}, an \code{edr_response}
wrapping GeoJSON, or a GeoJSON \code{FeatureCollection}.}

\item{data}{Optional station observations: a tidy tibble with a location-id
column, a named list of tibbles keyed by feature id, or \code{NULL} for
attribute-only popups.}

\item{popup}{One of \code{"plot+csv"} (default), \code{"plot"}, \code{"csv"},
\code{"table"}, or \code{"all"}.}

\item{location_col}{Column in \code{data} carrying the location id when
\code{data} is a single tibble. Default \code{"coverage_id"}.}

\item{id_col}{Column in \code{locations} to join on. If \code{NULL}, the
function looks for \code{"id"} then \code{"_id"} then the first character
column.}

\item{label_col}{Column in \code{locations} used for the popup heading.
If \code{NULL}, tries \code{"name"}, \code{"locationName"}, \code{"title"}, then the
detected id column.}

\item{parameter}{Optional character vector restricting the rows of \code{data}
used in popups. Filtered rows also determine whether a station is marked
as having data.}

\item{plot_width, plot_height}{Popup chart dimensions in inches.
Display size in pixels is \code{plot_width * plot_dpi} by
\code{plot_height * plot_dpi}, with a larger minimum size for readable
interactive popups.}

\item{plot_dpi}{Display dots-per-inch for popup charts. Default 72;
bump to 90+ if popups look small on hi-DPI displays.}

\item{marker_radius}{Marker radius in pixels for stations that have
time-series data. Data-less stations are drawn one pixel smaller.}

\item{matched_color}{Marker colour for stations that joined to a
coverage in \code{data}. Default deep blue.}

\item{unmatched_color}{Marker colour for stations without data
(only relevant when \code{data} is supplied and \code{show_unmatched = TRUE}).
Default light grey.}

\item{show_unmatched}{If \code{TRUE} (default), data-less stations are
drawn in \code{unmatched_color} so the user can see the full station
network. Set to \code{FALSE} to drop them entirely. Ignored when
\code{data} is \code{NULL}.}

\item{legend}{If \code{TRUE} (default), add a legend distinguishing
stations with data from those without. Suppressed automatically
when there are no unmatched markers to label.}

\item{max_match_distance}{Optional maximum coordinate distance for
spatially matching \code{data} rows with \code{x} / \code{y} columns to stations.
Units are those of the station coordinates. \code{NULL} (default) keeps
the nearest-station fallback unlimited.}

\item{group}{Optional Leaflet layer-group name. The default, \code{"Stations"},
makes the added markers available to \code{\link[leaflet:addLayersControl]{leaflet::addLayersControl()}}. Pass
\code{NULL} to retain the status groups used by a standalone station map
(\code{"Has data"} and \code{"No data in window"}).}

\item{fit}{If \code{TRUE}, fit the map to the added station extent. Defaults to
\code{FALSE} so adding stations does not replace an existing coverage extent.}
}
\value{
The updated \code{leaflet} htmlwidget.
}
\description{
Adds monitoring-location markers and the same interactive chart, CSV, and
attribute popups used by \code{\link[=edr_map]{edr_map()}} to an existing Leaflet widget. This is
useful for composing station observations over a gridded coverage map:
start with \code{edr_map(grid)} and add one or more station groups.
}
\details{
Multiple calls can add independently styled or toggleable source groups.
Coverage maps place their cells in a lower Leaflet pane so these station
markers remain visible and clickable.
}
