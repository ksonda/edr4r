% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{edr_plot}
\alias{edr_plot}
\title{Plot an EDR time-series response as a ggplot}
\usage{
edr_plot(
  data,
  parameter = NULL,
  group = "coverage_id",
  facet = "parameter",
  scales = "free_y",
  geom = c("line", "point", "both"),
  facet_labels = TRUE
)
}
\arguments{
\item{data}{Either a tidy tibble from \code{\link[=covjson_to_tibble]{covjson_to_tibble()}} or an
\code{edr_response} / \code{edr_covjson} object (which we flatten with
\code{\link[=covjson_to_tibble]{covjson_to_tibble()}} for you).}

\item{parameter}{Optional character vector restricting to a subset
of parameters.}

\item{group}{Column in \code{data} used for the colour aesthetic.
Defaults to \code{"coverage_id"} (one colour per location). Set to
\code{NULL} to disable.}

\item{facet}{Column to facet by. Defaults to \code{"parameter"} so each
variable gets its own panel; pass \code{NULL} to plot everything on
one axis.}

\item{scales}{\code{facet_wrap()} scales argument. Default \code{"free_y"}
gives each parameter its own y-axis range.}

\item{geom}{One of \code{"line"}, \code{"point"}, or \code{"both"}.}

\item{facet_labels}{If \code{TRUE} (default), facet strip labels include
the unit (e.g. \code{"discharge (ft3/s)"}).}
}
\value{
A \code{ggplot} object.
}
\description{
Convenience wrapper around \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} for the long tibble
returned by \code{\link[=covjson_to_tibble]{covjson_to_tibble()}}. By default each parameter gets its
own facet (so different units don't share a y-axis), and each
location is drawn in its own colour.
}
\examples{
\dontrun{
cl <- edr_client("https://api.wwdh.internetofwater.app")
resp <- edr_location(cl, "rise-edr",
                     location_id    = 3514,
                     datetime       = "2023-01-01/2023-06-30",
                     parameter_name = "3")
edr_plot(resp)
}
}
