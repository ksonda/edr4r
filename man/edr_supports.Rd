% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capabilities.R
\name{edr_supports}
\alias{edr_supports}
\title{Test an advertised EDR capability}
\usage{
edr_supports(
  x,
  collection_id = NULL,
  instance_id = NULL,
  query = NULL,
  format = NULL,
  conformance = NULL,
  refresh = FALSE
)
}
\arguments{
\item{x}{An \code{\link[=edr_client]{edr_client()}} or an \code{edr_capabilities} snapshot.}

\item{collection_id}{Collection to inspect when \code{x} is a client. Required
for query or format checks unless \code{x} is already collection/instance
scoped.}

\item{instance_id}{Optional instance to inspect. Requires \code{collection_id}
when \code{x} is a client.}

\item{query}{Optional scalar query type, such as \code{"cube"} or \code{"position"}.}

\item{format}{Optional scalar output format or media type.}

\item{conformance}{Optional conformance URI, namespace shorthand, or
unambiguous final URI component.}

\item{refresh}{If \code{TRUE}, bypass and replace cached discovery metadata.}
}
\value{
A single logical value. Multiple criteria use AND semantics.
\code{FALSE} means the capability was not advertised in the inspected
metadata; it does not prove the server cannot implement it.
}
\description{
Query names are matched case-insensitively. Format matching recognizes
common names and media types, including MIME parameters. With \code{query} and
\code{format}, query-specific formats take precedence and collection-level
formats are used only when the query advertises none. With \code{format} alone,
all top-level and query-specific formats are searched.
}
\details{
Conformance checks accept full URIs, unambiguous final components such as
\code{"covjson"}, and namespace shorthand such as \code{"edr/core"} or
\code{"common/core"}. Bare \code{"core"} is rejected because multiple OGC API
standards advertise a core conformance class.
}
