% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instances.R
\name{edr_instances}
\alias{edr_instances}
\alias{edr_instance}
\title{List instances of an EDR collection}
\usage{
edr_instances(client, collection_id, refresh = FALSE)

edr_instance(client, collection_id, instance_id, refresh = FALSE)
}
\arguments{
\item{client}{An \code{\link[=edr_client]{edr_client()}}.}

\item{collection_id}{Collection identifier as advertised by the server.}

\item{refresh}{If \code{TRUE}, bypass and replace cached instance metadata.}

\item{instance_id}{Instance identifier as advertised by \code{\link[=edr_instances]{edr_instances()}}.
Reserved characters are percent-encoded. A literal \code{/} is rejected
because it cannot safely round-trip as one HTTP path segment.}
}
\value{
\code{edr_instances()} returns a tibble with one row per instance. It
adds \code{collection_id} to the normalized metadata columns returned by
\code{\link[=edr_collections]{edr_collections()}}, keeping extent and output CRS semantics aligned.

\code{edr_instance()} returns the parsed instance metadata as a list.
}
\description{
Retrieves metadata for the versions or runs advertised beneath
\code{GET /collections/{collection_id}/instances}. Discovery responses use the
client's in-memory cache; set \code{refresh = TRUE} to bypass and replace the
cached value.

Retrieves the raw instance document from
\code{GET /collections/{collection_id}/instances/{instance_id}}.
}
