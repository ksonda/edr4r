% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{edr_map}
\alias{edr_map}
\title{Map EDR locations or coverage data}
\usage{
edr_map(
  locations,
  data = NULL,
  popup = c("plot+csv", "plot", "csv", "table", "all"),
  location_col = "coverage_id",
  id_col = NULL,
  label_col = NULL,
  parameter = NULL,
  plot_width = 7,
  plot_height = 3.5,
  plot_dpi = 72,
  tile_provider = "CartoDB.Positron",
  marker_radius = 6,
  matched_color = "#2C7FB8",
  unmatched_color = "#BBBBBB",
  show_unmatched = TRUE,
  legend = TRUE,
  max_match_distance = NULL,
  mode = c("auto", "stations", "grid", "profile"),
  controls = TRUE,
  initial = list(),
  grid_opacity = 0.75
)
}
\arguments{
\item{locations}{An \code{sf} object from \code{\link[=edr_locations]{edr_locations()}}, an
\code{edr_response} wrapping GeoJSON, or tidy coverage data from
\code{\link[=covjson_to_tibble]{covjson_to_tibble()}} / a CoverageJSON \code{edr_response}.}

\item{data}{See above. Defaults to \code{NULL}.}

\item{popup}{One of \code{"plot+csv"} (default), \code{"plot"}, \code{"csv"},
\code{"table"}, or \code{"all"}.}

\item{location_col}{Column in \code{data} carrying the location id when
\code{data} is a single tibble. Default \code{"coverage_id"}.}

\item{id_col}{Column in \code{locations} to join on. If \code{NULL}, the
function looks for \code{"id"} then \code{"_id"} then the first character
column.}

\item{label_col}{Column in \code{locations} used for the popup heading.
If \code{NULL}, tries \code{"name"}, \code{"locationName"}, \code{"title"}, then the
detected id column.}

\item{parameter}{Optional character vector restricting which
parameters are displayed. On station maps, the filtered rows also
determine whether a station is marked as having data. On coverage
maps, only matching rows are included in the widget payload.}

\item{plot_width, plot_height}{Popup chart dimensions in inches.
Display size in pixels is \code{plot_width * plot_dpi} by
\code{plot_height * plot_dpi}, with a larger minimum size for readable
interactive popups.}

\item{plot_dpi}{Display dots-per-inch for popup charts. Default 72;
bump to 90+ if popups look small on hi-DPI displays.}

\item{tile_provider}{Leaflet basemap. Default \code{"CartoDB.Positron"}.}

\item{marker_radius}{Marker radius in pixels for stations that have
time-series data. Data-less stations are drawn one pixel smaller.}

\item{matched_color}{Marker colour for stations that joined to a
coverage in \code{data}. Default deep blue.}

\item{unmatched_color}{Marker colour for stations without data
(only relevant when \code{data} is supplied and \code{show_unmatched = TRUE}).
Default light grey.}

\item{show_unmatched}{If \code{TRUE} (default), data-less stations are
drawn in \code{unmatched_color} so the user can see the full station
network. Set to \code{FALSE} to drop them entirely. Ignored when
\code{data} is \code{NULL}.}

\item{legend}{If \code{TRUE} (default), add a legend distinguishing
stations with data from those without. Suppressed automatically
when there are no unmatched markers to label.}

\item{max_match_distance}{Optional maximum coordinate distance for
spatially matching \code{data} rows with \code{x} / \code{y} columns to stations.
Units are those of the station coordinates. \code{NULL} (default) keeps
the nearest-station fallback unlimited.}

\item{mode}{Map mode. \code{"auto"} (default) uses station markers for
spatial feature inputs, grid cells for gridded coverage data, and
profile markers for vertical profiles. Use \code{"stations"}, \code{"grid"},
or \code{"profile"} to force a mode.}

\item{controls}{If \code{TRUE} (default), coverage maps include in-map
controls for available slice dimensions (\code{parameter}, \code{datetime},
and \code{z} for grids).}

\item{initial}{Named list of initial coverage-map selections, e.g.
\code{list(parameter = "temperature", datetime = "2024-01-01", z = 0)}.}

\item{grid_opacity}{Fill opacity for gridded coverage cells.}
}
\value{
A \code{leaflet} htmlwidget. Pass it to \code{\link[=edr_save_html]{edr_save_html()}} to
write a selfcontained HTML file.
}
\description{
Builds a \link[leaflet:leaflet]{leaflet::leaflet} map of station features or gridded/profile
CoverageJSON data. Station maps can show per-station popups with
interactive time-series charts and CSV downloads. Coverage maps keep
all supplied parameters, times, and vertical levels in the widget and
expose in-map controls for choosing the active slice; grid cells open
popups with a time-series chart for the clicked cell.
}
\details{
\code{data} can be one of:
\itemize{
\item \code{NULL} -- just markers with the sf attribute table as a popup
(when \code{popup = "table"} or \code{popup = "all"}).
\item A long tibble (the output of \code{\link[=covjson_to_tibble]{covjson_to_tibble()}}) with one
column matching the locations' id column. Set \verb{location_col =}
to the column in \code{data} that holds the location id and \verb{id_col =}
to the column in \code{locations}.
\item A named list of tibbles, keyed by feature id. This is what
\code{\link[=edr_explore]{edr_explore()}} passes when it fetches one time series per
station — and the right shape when each station has its own
CovJSON response, because server-assigned \code{coverage_id}s like
\code{"1"} won't naturally match the feature id.
}
}
