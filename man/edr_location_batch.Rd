% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{edr_location_batch}
\alias{edr_location_batch}
\title{Fetch data for multiple EDR locations}
\usage{
edr_location_batch(
  client,
  collection_id,
  location_id,
  datetime = NULL,
  parameter_name = NULL,
  z = NULL,
  crs = NULL,
  format = c("covjson", "csv"),
  ...,
  chunk = NULL,
  deduplicate = TRUE,
  checkpoint = NULL,
  resume = FALSE,
  include_parameters = FALSE,
  max_requests = 100L,
  on_error = c("stop", "collect"),
  progress = interactive(),
  instance_id = NULL,
  f = NULL
)
}
\arguments{
\item{client}{An \code{\link[=edr_client]{edr_client()}}.}

\item{collection_id}{Collection identifier.}

\item{location_id}{A non-empty character vector of unique location ids.}

\item{datetime}{Optional ISO-8601 instant or interval. With \code{chunk = NULL},
it is shared by every request; otherwise the bounded interval is split
into per-request windows. Timestamp bounds are normalized to UTC before
calendar arithmetic; up to six fractional-second digits are accepted
when R can preserve them without loss.}

\item{parameter_name}{Optional character vector of parameter names shared
by every request.}

\item{z}{Optional vertical level filter.}

\item{crs}{Optional CRS URI for the response.}

\item{format}{Either \code{"covjson"} (default) or \code{"csv"}.}

\item{...}{Additional query parameters forwarded to every
\code{\link[=edr_location]{edr_location()}} request, such as \code{limit}.}

\item{chunk}{Optional positive-integer calendar interval such as \code{"1 day"},
\code{"2 weeks"}, \code{"1 month"}, or \code{"1 year"}. Requires a bounded \code{datetime}
interval. Month/year boundaries use anchored calendar arithmetic, so a
January 31 start advances to February 28/29 and then March 31.}

\item{deduplicate}{If \code{TRUE} (default), exact rows repeated by different
time windows for the same location are retained only from the earliest
request. Duplicates within one response, differing observations, and
rows from different locations are preserved. Ignored when \code{chunk} is
\code{NULL}.}

\item{checkpoint}{Optional directory used to persist each terminal
successful or empty response. A new or empty directory is initialized
after the complete request plan has passed validation. Checkpoints store
parsed response data, but not client headers, query URLs, or errors.}

\item{resume}{If \code{TRUE}, reuse terminal responses in an existing compatible
\code{checkpoint} and request only unresolved rows. If the directory does not
yet exist, it is initialized, which supports rerunnable scripts. An
existing checkpoint requires \code{resume = TRUE}. Defaults to \code{FALSE}.}

\item{include_parameters}{If \code{TRUE}, fetch the collection's full parameter
catalog with \code{\link[=edr_parameters]{edr_parameters()}} and attach it once as \code{parameters} on the
result. This is an explicit, cacheable discovery request in addition to
the planned data requests. For an instance-scoped batch, metadata comes
from that instance. Defaults to \code{FALSE}, which performs no metadata
request and stores \code{NULL} in \code{parameters}.}

\item{max_requests}{Finite positive integer limiting the number of logical
\code{\link[=edr_location]{edr_location()}} calls in the complete plan. Transport-level retries do
not increase this count. Defaults to 100.}

\item{on_error}{Either \code{"stop"} (default), which re-signals the original
condition immediately, or \code{"collect"}, which records failures and
continues through the bounded request plan.}

\item{progress}{If \code{TRUE}, display a cli progress bar for multi-request
batches. Defaults to \code{\link[=interactive]{interactive()}}.}

\item{instance_id}{Optional collection instance identifier. Every request
remains beneath that instance path.}

\item{f}{Optional server-advertised output-format token sent as the EDR
\code{f} query parameter. This is separate from \code{format}, which controls
client-side parsing.}
}
\value{
An object of class \code{edr_location_batch} and \code{edr_batch}. It contains
\code{requests}, a typed request-status tibble whose \code{n_rows} values describe
raw responses before cross-window deduplication; \code{data}, a combined data
tibble; \code{errors}, a typed tibble of collected conditions; and \code{parameters},
either the nonduplicated collection/instance parameter catalog or \code{NULL}
when it was not requested. The object also records \code{collection_id},
\code{instance_id}, and \code{format}.
}
\description{
Runs one or more \code{\link[=edr_location]{edr_location()}} requests for each explicitly supplied
location id. When \code{chunk} is supplied, a bounded datetime interval is split
into contiguous closed windows and the complete station-by-window plan is
validated before any network activity. Requests remain sequential and in
input order, and \code{max_requests} guards the expanded plan against accidental
fan-out.
}
\details{
\code{format = "covjson"} responses are converted with
\code{\link[=covjson_to_tibble]{covjson_to_tibble()}}. CSV responses are already parsed as tibbles by
\code{\link[=edr_location]{edr_location()}}. Successful rows are combined with \code{.request_id} and
\code{.location_id} provenance columns.

Checkpoint requests remain sequential. Result files are written atomically
after parsing and before a request is marked complete in memory. Errors are
deliberately not terminal: a later call with \code{resume = TRUE} retries them
under the client's normal retry policy. A checkpoint may contain the
endpoint's returned observations, so protect it like any other local data
extract and resume it under the same logical authorization context.
Checkpointed clients must use an absolute HTTP(S) base URL without an
embedded query, fragment, username, or password; rotating credentials
belong in \code{client} headers and are not written to the checkpoint.

\code{max_requests} counts data requests only. When \code{include_parameters = TRUE},
the additional discovery request is made after an existing checkpoint has
been validated and restored. The catalog is not stored in the checkpoint,
so a resumed call obtains current metadata (subject to the client's cache)
even when every data response is restored. Parameter-discovery failures are
not collected by \code{on_error}; they abort the call because the requested
result metadata would be incomplete.
}
