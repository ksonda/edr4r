% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{edr_client}
\alias{edr_client}
\title{Create an EDR client}
\usage{
edr_client(
  base_url,
  user_agent = NULL,
  timeout = 60,
  max_tries = 3,
  retry_on_failure = TRUE,
  cache_ttl = 300,
  headers = NULL,
  verbose = FALSE
)
}
\arguments{
\item{base_url}{Base URL of an
\href{https://ogcapi.ogc.org/edr/}{OGC API - EDR} service. Examples:
the \href{https://api.waterdata.usgs.gov/ogcapi/beta/}{USGS waterdata OGC API}
at \code{"https://api.waterdata.usgs.gov/ogcapi/beta"}, the
\href{https://api.wwdh.internetofwater.app}{Western Water Datahub} at
\code{"https://api.wwdh.internetofwater.app"}, or
\code{"http://localhost:5005"} for a local \href{https://pygeoapi.io}{pygeoapi}
dev server. A trailing slash is optional.}

\item{user_agent}{String sent in the \code{User-Agent} header. Defaults to
\code{"edr4r/<version> (+https://github.com/ksonda/edr4r)"}.}

\item{timeout}{Request timeout in seconds. Defaults to 60.}

\item{max_tries}{Maximum number of attempts per request. The client
retries on 408, 429, and 5xx responses with exponential backoff.
Defaults to 3.}

\item{retry_on_failure}{If \code{TRUE} (default), retry low-level transport
failures such as connection resets and transient DNS / TLS errors. EDR
requests made by this package are read-only GET requests, so retrying
them is safe.}

\item{cache_ttl}{Number of seconds to retain discovery responses in this
client's in-memory cache. Defaults to 300 (five minutes). Use \code{0} to
disable caching or \code{Inf} to retain metadata until \code{\link[=edr_cache_clear]{edr_cache_clear()}} is
called. Data-query responses are never cached.}

\item{headers}{Named character vector of extra headers attached to
every request (e.g. \code{c(Authorization = "Bearer ...")}).}

\item{verbose}{If \code{TRUE}, prints request URLs to the console as they
are made. Useful for debugging.}
}
\value{
An object of class \code{edr_client}.
}
\description{
Builds a reusable client object that captures the base URL of an OGC
API - EDR service, a default user-agent, and HTTP options that are
applied to every request.
}
\examples{
usgs <- edr_client("https://api.waterdata.usgs.gov/ogcapi/beta")
usgs
}
